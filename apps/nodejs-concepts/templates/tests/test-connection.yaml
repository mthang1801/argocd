apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejs-concept.fullname" . }}-test-connection"
  labels:
    {{- include "nodejs-concept.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejs-concept.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
